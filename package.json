{
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "workflow-template": "bin/workflow.js"
  },
  "scripts": {
    "check": "node --check bin/workflow.js && node --check lib/installer.js && node --check lib/orchestrator.js && node --check lib/validator.js",
    "test": "node --test"
  },
  "dependencies": {
    "ajv": "^8.20.0",
    "yaml": "^2.9.0"
  }
}
